services:
  bolletrold-local:
    image: nginx:alpine
    container_name: bolletrold-local
    ports:
      - "8080:80"
    volumes:
      - ../www:/usr/share/nginx/html:ro
    restart: unless-stopped
